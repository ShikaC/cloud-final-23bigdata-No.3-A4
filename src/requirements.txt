# =============================================================================
# Python依赖包列表
# =============================================================================
# 用于虚拟化与容器性能对比实验的可视化和分析功能
#
# 安装方法:
#   方式1（推荐）: 使用虚拟环境
#     python3 -m venv venv
#     source venv/bin/activate  # Linux/Mac
#     pip install -r requirements.txt
#
#   方式2: 全局安装（不推荐）
#     pip3 install -r requirements.txt
#
# 更新依赖:
#   pip install --upgrade -r requirements.txt
# =============================================================================

# 数据可视化
# plotly: 交互式绘图库，生成HTML图表，避免中文字体问题
plotly>=5.0.0
# nbformat: plotly的依赖，用于导出
nbformat>=5.1.0

# 数据处理
# pandas: 数据分析库，用于读取和处理CSV文件
pandas>=1.3.0,<3.0.0

# numpy: 数值计算库，pandas的依赖
numpy>=1.21.0,<2.0.0

# 字体支持（可选）
# pillow: 图像处理库，matplotlib的依赖
pillow>=8.0.0,<11.0.0

# 类型检查和代码质量工具（开发时使用，可选）
# mypy>=0.910  # 静态类型检查
# pylint>=2.10.0  # 代码风格检查
# black>=21.0  # 代码格式化

